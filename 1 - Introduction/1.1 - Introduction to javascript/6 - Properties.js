// When you add a new piece of data into a JavaScript program, the browser saves it as a data type.
// Every string instance has a property called length that stores the number of characters that that string has
console.log('WHAT!?'.length)

// The . is another operator, it is called the dot operator