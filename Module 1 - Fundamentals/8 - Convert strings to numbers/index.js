// NaN = Not a Number
const age = "18";

console.log(age, typeof age);

const transformation = pareseInt(age); // pareseInt(age) is a function / It also removes all the non number characters

// Unary plus operator
// const example = +age; -- Same as parseInt()

console.log(transformation, age);